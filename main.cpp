#include "OS.cpp"

using namespace std;

int main() {
	
	// need to replace filename with command line parameter
	// config.txt should open the file which contains the 
	// file with the configuration settings.
	OS operatingSystem( "config.txt" );
	
	return 0;
}
